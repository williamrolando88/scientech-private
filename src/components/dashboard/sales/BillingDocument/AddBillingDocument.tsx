import { Button } from '@mui/material';

const AddBillingDocument = () => {
  const buttonCaption = 'Cargar factura';

  return <Button variant="contained">{buttonCaption}</Button>;
};

export default AddBillingDocument;
