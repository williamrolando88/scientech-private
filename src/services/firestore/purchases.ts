import { Purchase } from '@src/types/purchases';

const create = (purchase: Purchase) => {};

const update = () => {};

const remove = () => {};

const pay = () => {};

const unpay = () => {};

export const PurchasesFirestore = {
  create,
  update,
  remove,
  pay,
  unpay,
};
